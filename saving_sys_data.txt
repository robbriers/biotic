# current tests

data<-read.csv(file.choose(),header=TRUE)

test<-calcBMWP(data)
test

test2<-calcindex(data, index="PSI")
test2



data<-read.csv(file.choose(),header=TRUE)
data

test2<-calcindex(data, index="BMWP")
test2

test2<-calcindex(data, index="Whalley")
test2

test2<-calcindex(data, index="Riffle")
test2

test2<-calcindex(data, index="Pool")
test2

calcindex(data, index="Intermed")


###############################

# make sure BMWP.rda loaded first - not needed as loaded via sysdata.rda
BMWPtab<-read.csv(file.choose(), header=TRUE)
BMWPtab
# only needed to set up sysdata.rda - need to add family and species tables here
save(scoretab, file="R/sysdata.rda")
